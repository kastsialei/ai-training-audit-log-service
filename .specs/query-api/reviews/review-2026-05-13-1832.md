# Spec self-eval — query-api

- **Gate:** Gate 2 (requirements + design + tasks)
- **Date:** 2026-05-13 18:32 EEST
- **Reviewer:** spec-self-eval skill
- **Spec snapshot:**
  - requirements.md — present
  - design.md — present
  - tasks.md — present
  - glossary.md — present
  - HEAD commit: `78cf6a8` (uncommitted changes)

## A. Acceptance criteria — WEAK
Evidence: The new `requirements.md → US-1` actor-list ACs are observable and testable, but `requirements.md → Open questions` still asks whether the proposed OR semantics, whitespace handling, duplicates, repeated params, and cursor canonicalization are final.

## B. Tasks & Definition of Done — FAIL
Evidence: `requirements.md → US-1` and `US-3` now add multi-actor behavior, but `tasks.md` has no task/DoD covering actor-list parsing, validation, query filtering, cursor fingerprinting, or pagination tests, leaving orphan ACs.

## C. Relevance & scope — PASS
Evidence: The change stays inside the query endpoint's existing actor filter and pagination scope, and all new unresolved points are captured in `requirements.md → Open questions` with owners and a required phase.

## D. Edge cases & failure modes — FAIL
Evidence: `requirements.md → Open questions` explicitly leaves repeated `actor` params, whitespace around commas, duplicate values, and actor-list cursor canonicalization unresolved, while `design.md → §6 "Edge cases & failure modes"` only specifies single-value actor behavior.

## E. Architecture quality — FAIL
Evidence: `design.md → §2 "Query parameters"` and `glossary.md → §2 "QueryCriteria"` still model `actor` as a single exact-match string, so the cross-layer contract no longer matches the updated requirements.

Verdict: blocked
