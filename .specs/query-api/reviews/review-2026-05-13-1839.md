# Spec self-eval — query-api

- **Gate:** Gate 2 (requirements + design + tasks)
- **Date:** 2026-05-13 18:39 EEST
- **Reviewer:** spec-self-eval skill
- **Spec snapshot:**
  - requirements.md — present
  - design.md — present
  - tasks.md — present
  - glossary.md — present
  - HEAD commit: `78cf6a8` (uncommitted changes)

## A. Acceptance criteria — PASS
Evidence: The new `requirements.md → US-1` and `US-3` actor-list ACs are observable Given/When/Then statements covering OR matching, max-before-dedup validation, repeated-param rejection, whitespace rejection, pagination, and cursor order equivalence.

## B. Tasks & Definition of Done — FAIL
Evidence: `requirements.md → US-1` and `US-3` now add multi-actor behavior, but `tasks.md` has no task/DoD covering actor-list parsing, validation, query filtering, cursor fingerprinting, or pagination tests, leaving orphan ACs.

## C. Relevance & scope — PASS
Evidence: The change stays inside the existing query endpoint's actor filter and pagination scope; resolved actor-list decisions are recorded in `requirements.md → Open questions`, while unrelated deferred work remains out of scope.

## D. Edge cases & failure modes — FAIL
Evidence: `requirements.md → US-1` specifies repeated-param and whitespace rejection plus max-before-dedup behavior, but `design.md → §6 "Edge cases & failure modes"` only specifies single-value actor behavior.

## E. Architecture quality — FAIL
Evidence: `design.md → §2 "Query parameters"` and `glossary.md → §2 "QueryCriteria"` still model `actor` as a single exact-match string, so the cross-layer contract no longer matches the updated requirements.

Verdict: blocked
