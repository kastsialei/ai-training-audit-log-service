# Example only. DO NOT commit real credentials.
# Apply manually or via sealed-secrets / external-secrets in real clusters.
apiVersion: v1
kind: Secret
metadata:
  name: audit-log-service-secret
  namespace: audit-log
type: Opaque
stringData:
  SPRING_DATASOURCE_USERNAME: "audit"
  SPRING_DATASOURCE_PASSWORD: "CHANGE_ME"
