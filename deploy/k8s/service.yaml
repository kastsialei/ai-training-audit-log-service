apiVersion: v1
kind: Service
metadata:
  name: audit-log-service
  namespace: audit-log
spec:
  type: ClusterIP
  selector:
    app: audit-log-service
  ports:
    - name: http
      port: 8080
      targetPort: http
